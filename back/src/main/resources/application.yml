spring:
  application:
    name: 1day 1baek
  config:
    import:
      - optional:classpath:/secrets.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  devtools:
    livereload:
      enabled: true
  security:
    oauth2.client:
      registration:
        github:
          client-id: fd567229463b6f2452fb

server:
  port: 8080

custom:
  client:
    domain: http://localhost
    port: 5173
